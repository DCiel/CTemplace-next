name: Docker Image CI to GHCR

on:
  push:
    branches: ["main"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        run: |
            docker login --username faes763 --password ${{ secrets.GH_PAT }} ghcr.io
            docker build -t ghcr.io/faes763/${{ github.event.repository.name }}:latest .
            docker push ghcr.io/faes763/${{ github.event.repository.name }}:latest
